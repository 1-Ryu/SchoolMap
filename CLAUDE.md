# 학교어디 (대구초등학교맵)

대구 초등학교 233곳을 지도에서 조건별로 찾아보는 웹. 전보 희망학교를 고르는
선생님들이 쓴다. 대부분 **폰으로 본다** — 판단이 갈릴 때는 폰을 기준으로 정한다.

배포: https://school-map-psi.vercel.app/

## 구조

정적 파일 3개가 전부다. 빌드 도구도 프레임워크도 없다.

| 파일 | 역할 |
|---|---|
| `templates/index.html` | 화면 뼈대. Vercel이 이 파일을 루트로 서빙한다 |
| `templates/app.js` | 지도·마커·시트·필터 전부 |
| `templates/style.css` | 색과 배치. 색은 `:root` 변수에 모여 있다 |
| `data/대구초등학교_데이터_좌표완료.csv` | 학교 233곳 |
| `main.py` | 로컬 확인용 서버. 배포에는 안 쓰인다 |

## 작업 방법

**로컬에서 확인**

```bash
python -m http.server 8777 --directory templates
```

→ `http://localhost:8777/index.html`

**`style.css`나 `app.js`를 고쳤으면 `index.html`의 `v=` 번호를 올린다.**
안 올리면 브라우저가 예전 파일을 계속 써서 고친 내용이 반영되지 않는다.
이것 때문에 "왜 안 바뀌지" 하고 한참 헤맨 적이 있다.

```html
<link rel="stylesheet" href="style.css?v=11">
<script src="app.js?v=11"></script>
```

**배포**

`git push` 하면 Vercel이 자동 배포한다. 1분 안에 반영된다.

간혹 GitHub이 Vercel에 알리는 신호가 누락되어 배포가 안 걸린다. Vercel의
Deployments 목록 맨 윗줄 커밋 번호가 방금 올린 것과 다르면 그 경우다.
빈 커밋(`git commit --allow-empty`)으로 다시 밀면 된다.

**네이버 지도 키**

`index.html`의 `ncpKeyId`는 **등록된 주소에서만** 작동한다. 새 주소(포트 포함)에서
지도를 띄우려면 NCP 콘솔 → Maps → 애플리케이션 → Web 서비스 URL에 추가해야 한다.
등록되지 않으면 `naver.maps` 객체는 만들어지는데 타일만 안 내려와서, 빈 회색
화면이 된다. 이 경우를 코드에서 감지해 안내하도록 해뒀다.

## 디자인 규칙

**색은 `style.css`의 `:root` 한 곳에서만 정한다.** 바꿀 일이 생기면 거기만 고친다.

- 강조색 `--accent: #E4622C`
- 옅은 강조 `--accent-soft: #FDEDE6` / 글자 `--accent-ink: #B84A22`

**진한 강조색은 눌러야 하는 것에만 쓴다.** 지금은 세 곳뿐이다 —
필터 개수 배지, `학교 보기`, 시트의 `자동차 길찾기`.
필터 칩처럼 여러 개가 동시에 켜지는 것은 **옅게 물들이기만** 한다.
전부 진하게 칠했더니 화면이 주황으로 가득 차 눈이 피로했다.

**급지 색** (`app.js`의 `GRADE_TONE`) — 한 계열 안에서 진하기 4단계.
색을 구분하지 못해도 진하기만으로 순서가 읽히게 했다. 숫자는 모두 흰색.

| 가 | 나 | 다 | 라 | 급지 없음(국립·사립) |
|---|---|---|---|---|
| `#A6390F` | `#E4622C` | `#F59F55` | `#FBD7AE` | `#C6C0B6` |

**칩 폭은 빠듯하다.** 글자를 늘리면 좁은 폰에서 줄이 갈린다. 대신 높이는
줄바꿈에 영향을 주지 않으니 안전하게 키울 수 있다. 새 조건을 넣을 때는
360px과 412px 양쪽에서 줄 수를 확인할 것.

## 성능상 건드리면 안 되는 것

세 가지 모두 실제로 폰에서 끊겨서 고친 것들이다. 되돌리면 증상이 재현된다.

**1. 마커는 SVG 이미지여야 한다** (`pinUrl`)
HTML 마커는 마커마다 DOM이 생겨서 233개가 깔리면 확대/축소가 버벅인다.
이미지 아이콘은 노드 1개로 끝난다.

**2. 지원청 경계선은 지도가 움직이는 동안 감춘다** (`showBoundaries`)
경계선은 화면을 덮는 SVG 도형 10개다. 매 프레임 그 넓이를 다시 칠하느라
폰에서 심하게 끊긴다. 점 개수를 90% 줄여도 소용없었다 — 병목은 **칠할 면적**이지
점 개수가 아니다. `dragstart`/`zoom_changed`에 숨기고 `idle`에 되살린다.
`setVisible`은 0ms지만 `setMap`은 30ms라, 반드시 `setVisible`을 쓴다.

**3. 시트 안쪽 스크롤은 가로채지 않는다**
끌기는 `.sheet-top`(손잡이·제목)에서만 받는다. 시트 전체에 `touch-action: none`을
걸었더니 안쪽 스크롤의 관성까지 죽어서 뻑뻑했다.
닫는 위치는 픽셀이 아니라 `translateY(100%)`로 잡는다. 픽셀로 굳히면 폰에서
주소창이 숨겨져 화면이 커질 때 시트 일부가 아래에 띠로 남는다.

## 하지 않기로 한 것

**직선거리를 소요시간으로 환산해 보여주지 않는다.**
"직선 7.4km → 차로 26분" 같은 값은 도로 우회·신호·정체를 반영하지 못한다.
대구는 신천·금호강·앞산 때문에 특히 어긋난다. 틀린 값을 정확한 척 보여주면
한 번 겪은 선생님은 서비스 전체를 못 믿는다.

직선거리는 **순위**로는 쓸 만하지만 **절대값**으로는 못 쓴다. 표시할 때도
"직선거리"임을 숨기지 않는다. 실제 소요시간이 필요하면 네이버 길찾기로 넘긴다.

## 우리집 위치

좌표를 `localStorage`(`schoolmap.home`)에만 저장한다. **서버에 올리지 않는다** —
로그인이 필요 없고, 우리가 집 주소를 보관하지도 않는다. 대신 기기마다 따로
저장되고 브라우저 데이터를 지우면 사라진다. 이 성질은 의도한 것이다.

정하는 방법은 둘이다. 버튼으로 배우고 익숙해지면 길게 누르게 된다.

- 우하단 🏠 버튼 → 안내 막대가 뜨고 → 지도를 누르면 지정
- 지도를 **길게 누르면** 곧바로 그 자리로 이동 (PC는 오른쪽 클릭)

지정하면 시트에 직선거리가 뜨고, 필터에 거리 제한이 열리고, 길찾기 출발지가
자동으로 채워진다. 지정 전에는 거리 슬라이더 대신 설정 버튼이 보인다.

## 앞으로

1. **학교 식별 코드 + 학교알리미 데이터** — 학생수, 3년 추이(폐교 위험·과밀 신호),
   교원수 등. **먼저 표준학교코드를 CSV에 넣어야** 나중에 어떤 자료를 붙여도
   이름 표기 차이로 깨지지 않는다.
2. **조건 기반 추천** — 가중치 점수로 충분하다. 근거를 보여줄 수 있어야 하므로
   설명 못 하는 방식은 쓰지 않는다.
3. (한참 뒤) 근무 후기 — 익명 리뷰로 **사람**을 평가하면 명예훼손 위험이 실재한다.
   업무량·통근·시설 같은 **구조**만 항목으로 받는 설계여야 한다. 교사 인증이
   필요해지는 시점이고, 로그인이 처음 정당화되는 지점이기도 하다.
